package com.fitzone.fitzone.repository.customer;

//import com.fitzone.fitzone.dto.request.SearchRequest;
import com.fitzone.fitzone.entity.ProductEntity;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Repository;

@Repository
public interface ProductRepositoryCustomer {
   // Page<ProductEntity> search(SearchRequest request, Pageable pageable);
}
